#要使用apt卸载一个包，你可以使用以下命令：
sudo apt remove package_name#其中，package_name是你要卸载的软件包的名称。
#如果你希望同时删除软件包的配置文件，可以使用purge命令：
sudo apt purge package_name

sudo apt purge openjdk-17-jdk
#安装JAVA环境
sudo apt update
sudo apt install openjdk-17-jdk
#sudo apt install openjdk-11-jdk
java -version#打印java版本
sudo apt install sdkmanager#安装许可证管理包
sdkmanager --licenses#运行命令来接受所有SDK许可证
#指定环境变量【可能不对】
echo $ANDROID_HOME#查看安卓SDK位置
export ANDROID_HOME=/home/wth000/.android#设置安卓SDK位置
sudo apt update
sudo apt install android-sdk

#下载并编译目标文件
git clone --recurse-submodules https://git.zx2c4.com/wireguard-android
cd wireguard-android
./gradlew assembleRelease